# Product Knowledge: Zoho CRM ⇌ Zoho Survey Integration

This file gives Claude the product context needed to work on this codebase. It is
knowledge, not engineering convention — see other docs (once added) for tech stack
and coding rules.

## What this product is

A Zoho Survey experience embedded inside Zoho CRM, letting a CRM user create surveys,
send them to CRM records (Leads/Contacts/custom modules), and sync responses back into
CRM as records or field updates. Reference mock: `mock-reference.html` (a static,
single-file HTML mock — screens, flow, and mock data only, not production code).

## Company context

Zoho is a business software company that builds a broad suite of interconnected apps
(CRM, Survey, Campaigns, Books, Desk, and dozens more), sold both standalone and as
part of a unified operating suite. The strategic bet behind this project is Zoho's
cross-product integration story: instead of a customer buying a survey tool,
manually exporting results, and re-importing them into their CRM, Zoho apps talk to
each other directly. Zoho Survey's full feature set already exists as a mature
standalone product — the job here is adapting and re-flowing those existing features
so they work naturally *from within* Zoho CRM, not building new survey functionality
from scratch.

## Primary persona

**Jay — Sales Rep / Account Executive**
- Lives in Zoho CRM daily; uses the embedded Survey feature end-to-end but only
  within CRM — he has never used standalone Zoho Survey and doesn't want to learn it
  as a separate product.
- **Creates** surveys himself (e.g. a post-deal CSAT survey, a periodic NPS check-in)
  using CRM-native language and his own CRM data/fields, not generic survey jargon.
- **Sends** surveys to his own Contacts/Leads directly from the CRM records or views
  he already works in — e.g. right after a deal moves to Closed-Won.
- **Watches analytics**: response rates, CSAT/NPS trends per account, who hasn't
  responded yet — reading results back on the record and in reporting, without
  leaving CRM to check a separate Survey dashboard.
- Technical comfort: strong in CRM concepts (modules, records, fields, tasks); no
  interest in survey-specific concepts (collectors, quotas, logic builders) beyond
  the minimum required to create, send, and read results.
- Frustration trigger: any step that feels like operating a second product (e.g. a
  collector-setup screen, a generic template picker) instead of an extension of the
  CRM he already knows.
- Also acts as the **CRM admin** for this integration — he's the one who sets up the
  Push to CRM config (field mapping, field value mapping, target module/layout,
  record assignment rules) as well, so no separate admin persona is needed.
- Design implication: Jay is the lens for the **entire surface** — survey creation,
  the Push to CRM admin config, the Send Survey wizard, and reporting/analytics. Every
  screen in this project should be designed against his needs and technical comfort.

## Zoho CRM concepts used here

- **Module**: a CRM object type — standard (Leads, Contacts, Accounts, Potentials) or
  custom. Surveys are sent to records that live inside a module.
- **Record**: a single row/entity within a module (e.g. one Lead).
- **Layout**: the field arrangement/form definition for a module — Standard or Custom
  layouts can differ in which fields exist.
- **Field**: a single data attribute on a module (First Name, Email, Phone, Company...).
  Some are required for the module (e.g. Last Name, Email on Leads).
- **Assignment Rules**: CRM automation that decides which user/team owns a new record.
  Relevant when survey responses create new CRM records and need an owner.
- **Users / Teams / Roles**: CRM's ownership and permission model. Record Assignment
  (below) can target any of these.
- **Workflows/Automation**: CRM's broader rule-based automation system. This mock does
  not model workflows explicitly — they only appear as generic dropdown options, not
  as a designed integration point (worth clarifying with the PM before building).

## Zoho Survey concepts used here

- **Survey**: the top-level object — has a name, category, type/template, appearance
  settings (logo, intro page), respondent-experience settings (timer, progress bar,
  captcha, quotas, save-and-continue), and completion/distribution settings (end page,
  disqualification page, redirect, social preview).
- **Collector**: a distribution channel/audience funnel for a survey — every response
  is tagged with the collector it came through. Collectors have their own name, quota,
  and auto-close behavior. Used both for sending (choose a collector to send through)
  and reporting (Responses by Collector). Created from the Launch tab → Collector List
  dropdown → "Add New Collector" → set a Collector Name → Save. Each collector gets its
  own web link, and that link updates automatically whenever the collector's settings
  are edited (quota, name, etc.) — existing distributions keep working off the same
  updated link rather than needing to be resent. Segmenting respondents into distinct
  collectors (e.g. by audience type) lets reporting break results down and compare
  across those segments
  ([source](https://help.zoho.com/portal/en/kb/survey/launch/collectors/articles/collectors)).
- **Question / Form Builder**: surveys are built page-by-page with questions; question
  editors support merge variables (e.g. "Collector Name") for personalization.
- **Response**: one respondent's submission — status (Completed/Partial/Over-Quota),
  timing (start/completion time, time taken), IP address, per-question answers, and —
  in the CRM-integrated context — an associated CRM module, record link, and metrics
  (NPS score, CSAT score, average rating, tags).
- **Logic/Condition builder**: used in quotas, end pages, and disqualification pages —
  Field + Condition (Equal to / Not equal to, etc.) + Value rows, evaluated against
  collector or answer data.
- **Custom Variables**: store known respondent info (e.g. from CRM) inside a survey
  without asking the respondent a question for it. Configured under Advanced Options →
  Custom Variables — each has a URL Parameter name, a Label, and a Type (Text, Email,
  Number, or Choice with 2+ options). Values are passed in via URL params on the survey
  link (`?email=...`, or `?var1=[value]&var2=[value2]` for multiple). Once set, a
  variable can be piped into question text for personalization, used in Display Logic
  like a question answer, and populated automatically via merge tags from Zoho CRM,
  Zoho Campaigns, or MailChimp — this is the mechanism most relevant to CRM record data
  flowing into a survey link without extra questions
  ([source](https://help.zoho.com/portal/en/kb/survey/survey-builder/advanced-options/custom-variables/articles/custom-variables)).

## How the two connect (the actual integration surface)

1. **Push to CRM** (survey-level toggle + "Manage Configuration" modal): the core
   config screen that defines how this survey talks to CRM. It has three distinct
   sub-concepts — don't conflate them:
   - **Field Mapping**: maps CRM fields (First Name, Last Name*, Email*, Phone,
     Company, City, State, Country) 1:1 against survey questions, so an answer fills
     a CRM field.
   - **Field Value Mapping**: sets a *static* default value for a CRM field (e.g.
     always set Company = "Test"), independent of any survey answer.
   - **Record Assignment**: decides who owns a CRM record created/updated from a
     response — either "Users matching certain conditions" or CRM's Assignment Rules.
   - Also configures target **module** (Leads/Contacts/custom) and **layout**
     (Standard/Custom), plus duplicate handling ("if a lead already exists":
     Skip vs Create New).
2. **Send Survey wizard**: Module Chooser (pick target CRM module) → Choose Collector
   (pick collector + URL params) → recipient picker (a filterable table of actual CRM
   records to send to, with search/select-all, and a "Create Lead" shortcut) → Mass
   Email compose (To/Template/From) → template selection.
3. **Response → CRM sync**: every response mock record carries `module`,
   `responseId`, `collector`, plus CRM-context fields (customercode, accounttype,
   country, phone) alongside survey metrics — this is the shape a response takes once
   synced back to CRM.
4. **Reporting ties back to CRM context**: "Responses by Module" and
   "Responses by Collector" reports segment survey results by which CRM module (and
   which collector) they came from — the two systems share a reporting lens, not just
   a data-push.

## Terminology quick-reference

| Term | Belongs to | Meaning |
|---|---|---|
| Module | CRM | Object type (Leads, Contacts, custom) |
| Layout | CRM | Field arrangement for a module |
| Assignment Rule | CRM | Automated record-ownership logic |
| Collector | Survey | Distribution channel / response funnel |
| Push to CRM | Integration | Survey→CRM sync config (mapping + assignment) |
| Field Mapping | Integration | Question → CRM field, dynamic |
| Field Value Mapping | Integration | CRM field ← static value |
| Record Assignment | Integration | Who owns the resulting CRM record |

## Working agreement

- After every meaningful update to the local build, open/refresh it in the browser
  (via the claude-in-chrome tools) at `http://localhost:8080` so the user can see the
  change live without doing it manually. Requires the Chrome extension connected
  (`/chrome`) — if unavailable, tell the user to refresh manually instead of skipping
  this silently.
- Every edit to `mock-reference.html` gets committed to git, and this file's Mock Edit
  Log (below) gets a new entry in the same commit describing what changed and why.
- After every commit, push to `origin/master` (the global master) right away — don't
  let commits sit local-only. If the push fails or is rejected, tell the user instead
  of silently skipping it.

## Mock Edit Log

Reverse-chronological log of edits made to `mock-reference.html`. Each entry: date,
one-line summary, why.

- 2026-08-31 — [branch `create-flow-v2`] **Gutted the Customer Voice dashboard and replaced it with two reports: "Feedback by Process" and "Feedback by User".** Removed everything that was there (at-risk hero band, at-risk deals worklist, detractor verbatim feed, objection themes, sentiment-by-touchpoint columns, trend tiles + recovery funnel, CSAT-trend lines, root-cause bars/pie, consultant leaderboard, promoters/referrals, testimonials) along with all `cvResp`/`CV_TP`/`CV_SENT`/`cvThemes` data and the `cvL/cvTag/cvStatus/cvSec/cvArrow/cvTpStats/cvReps` helpers. New model: a **process** is a business process that ends in a customer-facing moment (Deal → Closed Won, Job Card → Closed, Renewal → Renewed …), each sent by its own CRM workflow. **Report 1 — Feedback by Process**: 7 dealership processes (Service job, Insurance renewal, Delivery, Vehicle purchase, Ownership pulse, Lost sale, Enquiry handling) × Sent / Responded (rate) / CSAT / NPS / Rating / Score / Δ vs last period, sorted worst-first, with a Total-weighted row and **show-if-present dashes** (a process shows only the metrics its survey collects — the two loss processes carry reasons, not scores). **Report 2 — Feedback by User**: the same eight columns grouped by process, with **Δ vs that process's own average** (never the company average — a delivery exec at 3.2 is the worst in the dealership even though it beats Service's 3.1) and a **low-volume marker** (<10 responses shown greyed/italic and "not ranked"). Both cards carry CRM-style filter chips (Period/Survey/Module, and Period/Process/Filter-by-User). Added **Happy / Unhappy count columns** to *both* tables (right after Responded, green #1F8A54 / red #C8324A, each showing the count plus its share of responses via `cvMood()`) — an average hides the distribution (a 3.1 could be everyone mildly annoyed or half delighted and half furious) and the counts double as the size of the follow-up queue (Service job: 190 happy / 158 unhappy of 508). Happy/Unhappy = CSAT 4–5 vs 1–2 on the CSAT processes, promoters vs detractors on the NPS one; the two loss processes show dashes since their survey collects reasons, not satisfaction. Then added an **"Overall Feedback" band above both tables** (`cvBand()`) so the company-level answer to *good or bad?* is readable at a glance. Deliberately **not** a single blended CSAT — that number averages a 1–5 scale with an NPS process and is 54% Service by volume, so it is really the service department wearing a company badge. Instead the hero is the **happy / neutral / unhappy split** (counts are comparable across every process regardless of scale): a full-width gradient stacked bar + a 3-item legend (438 47% Happy / 305 32% Neutral / 198 21% Unhappy), plus a **Time Range** filter chip under the header (the delta chips vs last month and the `cvPrev`/`cvBandChip` code were removed in a follow-up — period comparison now belongs to the filter, not to fixed chips). Below a hairline divider, four left-accented tiles: **Responses** 38% responded (941 responded · 1,523 not responded — reworded per user from the earlier Coverage tile, which compared sends against completed process instances; the `done` field per process is still in the data but no longer surfaced, so the band no longer flags a workflow that has silently stopped firing), **Processes declining** 2 of 5, **Needs attention** (highest unhappy share — Service job), **Doing well** (highest happy share — Vehicle purchase). All figures derive from `cvProc`, so the band stays in sync with the tables. **Then made every filter chip actually work** — rewrote the whole render layer around a `cvF` state object (band: period; process report: period/survey/module; user report: period/process/user) plus `cvMenu`. Chips are now real dropdowns (`cvChip`/`cvChipClick`/`cvPick`, checkmark on the selected item, hover states, blue "active" styling when set to anything but All, closed by a document-level click handler) and every pick re-runs `renderAnalytics()`. **Period/Time Range is genuinely data-driven**: `CV_PERIODS` gives each range a volume multiplier + score adjustment + delta multiplier, applied by `cvScaleProc`/`cvScaleUser` — and *Last month* uses each process's own Δ inverted, so switching to it reproduces exactly the number the Δ column claims (Service job 3.1 ▼0.5 → 3.6 last month). Survey/Module filter the process rows (added a `survey` field per process); Process/User filter the user groups; both tables show a proper empty state when nothing matches. The Total row is now **computed from the visible rows** rather than hardcoded (`cvTotals`, replacing `cvTot`), which surfaced a real bug: weighting the Δ across all processes mixed NPS points (−6) with CSAT points and swamped the average — the roll-up now only includes CSAT-unit deltas (total reads ▼0.3, not ▼0.6). **Added a third report, "Feedback by Survey"** (`cvSurveyReport`, between Process and User), because the survey is the *instrument* while the process is the business: Survey (+ question count, linking via `cvOpenSurvey` to the existing survey detail page) / Used by (the processes it serves) / Sent / Responded / **Partial** / **Avg time** / Happy / Unhappy / CSAT / NPS / Rating / Score, with its own Period + Survey chips. Three rows — Experience CSAT (4 processes), Relationship NPS (1), Loss Reason (2) — from a new `CV_SURVEYS` map (questions, partial rate, avg minutes). Because one survey spans several processes, any metric averaged over more than one process is **greyed and asterisked** with a footnote pointing back to the per-process rows, so the blended 3.6* can never be mistaken for an actionable score. Two data-honesty fixes came out of building it: `cvTotals` now returns **null** (not 0) for happy/unhappy when no row in the group collects them — Loss Reason correctly shows dashes instead of "0 (0%)" — and it tracks a per-metric contributor count so the asterisk appears only when that *particular* metric was blended (Experience CSAT's NPS comes from Service alone, so it renders plain). Per follow-up, removed the Branch filter chip from both cards, both explanatory footnotes, both "Created from report - …" footers (and the now-unused `cvNote` helper), the low-volume treatment entirely, and the **Δ vs process** column — the user report is now User / Sent / Responded / CSAT / NPS / Rating / Score, with the process average still shown in each group header. Then added a **Modules** column beside Process (the module moved out of the name cell into its own left-aligned column; `cvTh` gained a `leftN` arg) and made each **process name a blue link to its workflow** (`.cv-proc-link`, #5464F2 → #3752CA + underline on hover, cursor pointer) — `cvOpenWorkflow(id)` hides the analytics overlay and opens that rule's Workflow-rule setup canvas. Added the 7 matching rules to `workflowRulesData` (ids 10–16: Post-Service Feedback @Service Requests, Renewal Feedback @Renewals, Post-Delivery Feedback @Deliveries, Post-Purchase Feedback @Potentials, 6-Month Ownership NPS @Vehicles [Date Based], Lost Deal Feedback @Potentials, Dropped Enquiry Feedback @Leads), each carrying an Email Notification instant action that sends the survey. Why: the manager's feedback was that surveys are sent at the **end** of a process (never mid-deal), and that the missing capability is seeing **which process** and **which person** feedback is positive or negative for — the old dashboard was built on mid-deal touchpoints and a deal-rescue "at-risk revenue" framing that is invalid once the deal is already won or lost. Verified live at localhost:8080 (2 cards, 7 process rows + total, 7 user groups, no console errors).
- 2026-08-31 — [branch `create-flow-v2`] **Added Happy / Unhappy tiles with drill-down to the survey detail page's Response Statistics.** Rebuilt the stat grid from 6 columns into **4×2** (hairline-gap grid: 1px gaps over an `#e8e8e8` background instead of per-cell border-right, so both rows divide cleanly) and appended **Happy Responses** (green, "CSAT 4–5") and **Unhappy Responses** (red, "CSAT 1–2"). Both tiles are clickable (`.sd-stat-click` hover tint + underline, "Click to view these responses" tooltip) and **reuse the page's existing cross-filter mechanism** — `sdFilterSentiment(kind)` → `sdApplyChartFilter(label, test)`, which sets the dismissible chip on All Respondents, filters the table and smooth-scrolls to it, exactly like the chart-to-table filtering already built for Responses-by-Module/Collector/Trend/CSAT. Sentiment classification uses **one metric per response by precedence — CSAT → NPS → Avg rating** (`sdFrac` parses '7/10'/'3.0/10'/'4' forms and normalises rating to a /5 scale; `sdSentiment` returns happy/neutral/unhappy or null). **Survey "Score" is deliberately excluded**: question scoring counts points for *what* was answered, not how the respondent felt, so a happy customer who skipped optional questions would read as unhappy. Precedence (rather than combining or an any-metric rule) keeps the three buckets mutually exclusive so they still sum to the response count, keeps the verdict explainable, and lets an NPS-only survey still produce happy/unhappy counts. A response carrying none of the three (the Loss Reason surveys) is not classified at all rather than counted as neutral. CSAT-before-NPS matches the dashboard, where a process shows CSAT as its headline unless it only collects NPS. Counts are **computed from `sdRespData`** (Happy 3, Unhappy 2 of 6) rather than hardcoded like the other six tiles, so the number on the tile always equals the number of rows the drill-down shows; `sdSetData` zeroes them for a fresh survey. Why: user asked for happy/unhappy in Response Statistics with a drill-down on click. NOTE: the user referenced "the latest screenshots in the Zoho Webinar integration" as the pattern to match — those weren't available in-session (no attachment, and the Figma needs auth), so this follows the mock's own established drill-down pattern; revisit if the webinar design differs.
- 2026-08-31 — [branch `create-flow-v2`] **Beautified all three dashboard tables** so a manager can grasp them without reading digits. Seven changes, applied consistently to Feedback by Process / by Survey / by User: **(1) a Sentiment column** — a 96px green/grey/red stacked mini-bar per row (`cvSentBar`, tooltip with the three shares), the same visual language as the Overall Feedback band, so rows can be ranked by eye and a *split* process (Service 190 happy / 158 unhappy) is distinguishable from a uniformly mediocre one; **(2) response rate as a micro-bar** (`cvRate`) — bold count + muted % over a 4px filled track, turning a mental division into a length comparison; **(3) primary-metric pills** (`cvPill`/`cvMetric`/`cvBand5`/`cvBandNps`, green ≥4.2 / amber ≥3.5 / red below, NPS green ≥30 / amber ≥0 / red below) applied **only to each row's primary metric** so the table doesn't become a heatmap — Ownership pulse pills its NPS, everything else its CSAT; **(4) extreme-row emphasis** (`cvEmph`) — the highest-unhappy row gets a faint red wash + 3px red inset accent, the highest-happy row green, every other row stays white; **(5) column-group dividers** (`CV_SEP`) separating Volume | Sentiment | Scores | Trend, so 12 columns stop reading as one run; **(6) tabular numerals + more row air** (12px padding, 13.5px semibold names, muted module/meta); **(7) row hover + whole-row click** (`.cv-row`) — process rows open their workflow rule and survey rows open the survey, not just the name link. Why: user asked how to make the tables hook attention and be graspable at a glance, and approved the package. Deliberately skipped (offered, not taken in this pass): 12-week sparklines and per-process icons. Avoided stacking zebra striping + row tints + accents, since three highlighting systems cancel each other out. Verified live at localhost:8080 (12/13/10 columns across the three tables, bars + pills + accents render, no console errors).
- 2026-08-25 — [branch `create-flow-v2`] **Replaced the confusing "At-Risk vs Recovered Revenue" dual-line chart** on the Customer Voice dashboard with an easier-to-read format (the dual-line mixed a weekly snapshot with a cumulative total on one axis — apples-to-oranges). New card = **two plain-language trend tiles** (At-Risk Revenue ₹96.3L ▼ / Recovered This Week ₹22.5L ▲, each with an "improving/worsening vs last week" arrow + a mini `anSpark()` sparkline) **+ a recovery funnel bar** ("Of ₹118.8L flagged at-risk this period, you've recovered 19%" with a green Recovered / red Still-at-risk split bar). Added `anSpark(points,color)` helper; `trendRisk` is now an HTML string (tiles+funnel) passed to the same `anCard`. Why: user asked to make the chart easier for the manager to understand — answers "how much is at risk / are we winning" at a glance and "what fraction have we saved" as a part-of-whole, without decoding a dual-axis line chart. Verified live at localhost:8080 (2 sparkline tiles + funnel; no console errors).
- 2026-08-24 — [branch `create-flow-v2`] **Simplified the Integration overview** per user: removed all fabricated social proof / puffery — the **"Built by Zoho"** + Free/SOC2/setup **trust chips**, the **4.8★ / 12,000+ orgs trust band**, the **impact-stat band** (3×/95%/0/1-click), and the **final CTA band**; removed the hero's **Enable/See-how CTAs** so the **only Enable Integration button is the top-right header one**. What remains is a clean, minimalist story: hero (Zoho Survey ⇌ CRM animated connector + headline + one-line promise + "works with Leads/Contacts/Deals" fact), **How it works** (4-step animated narrative), **Why teams enable it** (4 benefit cards), and the factual **"When you enable, this integration will"** block. Why: user asked to drop the built-by-Zoho/ratings puffery and extra CTAs and just tell the story with illustrations + features + benefits, simple and straightforward. Verified live at localhost:8080 (1 Enable button, no fabricated stats; no console errors).
- 2026-08-24 — [branch `create-flow-v2`] **Reworked the Integration overview (State A) into an industry-standard marketplace hook/landing page** (business-context-agnostic, generic Survey↔CRM value). Sections: **hero** (two-app tiles Zoho Survey ⇌ Zoho CRM with an animated bidirectional data-flow connector + ⇌ badge, bold "Turn customer feedback into revenue — automatically" headline, dual CTAs [Enable Integration / See how it works→smooth-scroll], trust chips: Built by Zoho · Free · SOC 2·GDPR · ~2-min setup), a **trust band** (4.8★ · 12,000+ orgs · works with Leads/Contacts/Deals & custom), **How it works** (the 4-step blueprint→auto-send→capture→win narrative), **Why teams enable it** (4 hover-lift benefit cards: zero manual work / feedback on record / act in the moment / measure impact), an **impact stat band** (3× · 95% · 0 · 1-click, gradient numbers), a **"When you enable, this integration will"** transparency block, and a **gradient final CTA** ("Ready to close the feedback loop?"). Added keyframes sintPacketRev/sintPop + card hover-lift + .sint-cta styles. State B (config settings) unchanged; all CTAs call `enableIntegration()`. Why: user asked to make the enable page a true industry-standard hookup page that instantly conveys the USP and drives enablement, dropping the specific business framing. Verified live at localhost:8080 (hero connector + all sections + 3 CTAs + animated packets/stats; no console errors).
- 2026-08-24 — [branch `create-flow-v2`] Split the Integration page into **two states**: (A) an **animated overview shown first** (sells what the integration offers) with an **"Enable Integration"** button top-right, and (B) the **config page** shown after enabling (reproduces the Zoho Survey Integration settings screenshot). **Reworked the hero animation into the use-case narrative** the user asked for — a 4-step flow: **Blueprint / Workflow fires** (Deal → Delivered) →(packet)→ **Survey auto-sent** (Post-Delivery CSAT) →→ **Response captured** (NPS 9 · CSAT 4.6) →→ **Deal saved & closed** (Closed Won · ₹12.6L, green check pulse) — illustrating auto-send-via-blueprint/workflow → capture response → close the deal. Kept the 4 USP cards; removed the config sections from the overview. **State B (`#sint-config`)** reproduces the settings screenshot: Setup rail (Data Administration / Marketplace[Zoho active] / Developer Hub), "Zoho Survey Integration" title + red **Disable** + Help, Email/Portal/Departments, "Add User to Zoho Survey Profiles" + Add Users + avatar, and **Zoho CRM Modules** ("Select the modules…", Select-All + 21-module magenta-checkbox grid: Leads/Contacts muted, Potentials…AjB checked). `enableIntegration()` overview→config, `disableIntegration()` config→overview. Why: user said the integration page should first show the user what the integration offers (with a blueprint/workflow→send→capture→close-deal animation) and an Enable-Integration button that reveals the config screenshot. Verified live at localhost:8080 (overview narrative + Enable → config with 21 modules + Disable; no console errors).
- 2026-08-24 — [branch `create-flow-v2`] Built the **Survey ⇌ CRM Integration page** in **Marketplace → Zoho → Zoho Survey** (wired the Survey card's "Manage" button → `openSurveyIntegration()`; `#survey-integration-page` overlay before `#analytics-page`). **Animated hero** illustrating the USP closed-loop: Customer answers →(CSS `sintPacket` moving packet)→ "Lands on the record" (pulsing NPS/CSAT/Rating chips, `sintPulse`) →(packet)→ "Rolls up to revenue" (₹66.2L at-risk); floating nodes (`sintFloat`), glowing Connected dot. **4 USP cards** (auto-linked >95% identity resolution / sentiment on the record / event-driven sending / revenue-weighted) with floating icons. Config sections: **① Connection & modules** (department, org, module toggles), **② Push to CRM** (target module·layout, 8 fields mapped, assignment, duplicate handling + Manage Configuration), **③ Metric mapping** (NPS/CSAT/Rating/Score → CRM fields), **④ Custom Variables** (recordId/email/module URL params — the auto-link mechanism), **⑤ Sync health** (animated `sintDash` 97% identity-resolution ring, 1,284 synced, last sync, 3 need attention). Why: user asked what belongs on the integration page and to implement it in Marketplace→Zoho→Survey with animations illustrating the integration USPs — the page centralizes Push-to-CRM/metric-mapping/custom-vars and adds the novel sync-health + an animated closed-loop that sells voice→context→revenue. Verified live at localhost:8080 (hero + packets + USP cards + all 5 config sections + animated ring; no console errors).
- 2026-08-24 — [branch `create-flow-v2`] **Reworked record-level survey signals to per-transition attribution** (per the research conclusion: on a single record a transition = one response, so show *which transition* each metric came from, not a blended average). Extracted a shared `cvRenderSignals(body, data, meta, unit)` used by both Deal and Contact detail: each metric flash card now shows its latest value + "from <survey/transition name> · latest of N", followed by a **"By transition"** breakdown (each survey row with its trigger/stage + metrics, or "Yet to respond" for pending). Rewrote `renderDealSurveys` to delegate to it. Also **made the sidebar "Contacts" clickable** (`openContactsListPage`) and built a **Contacts list** (`#ctl-page`, 8 sample car-context contacts: Name/Account/Email/Phone/Owner) → **Contact detail** (`#ctd-page`, header + "Zoho Survey · Signals" running the same `cvRenderSignals` on a contact-appropriate `contactSurveysData`: Onboarding CSAT / Post-Service CSAT / Relationship NPS / Renewal-Pulse[pending]) + a Contact Details strip. Why: user asked to rework the record signals (per-transition attribution, from the multi-module analytics research) and to make Contacts clickable and show these metrics inside the Contact detail too. Verified live at localhost:8080 (deal + contact both show per-transition attribution + By-transition breakdown; contacts list 8 rows; no console errors). Note: per the research, per-transition *charts* were deliberately NOT added on records (n=1); analytics-by-transition stays at the aggregate/manager level.
- 2026-08-24 — [branch `create-flow-v2`] Made **Settings → Process Management → "Blueprint"** clickable (`openBPList()`) and reproduced the 4 Blueprint screenshots as overlays (`#bp-list`/`#bp-create-modal`/`#bp-canvas` + `<script>`, before `#analytics-page`): (1) **Blueprint list** — Setup left rail (Automation/Process-Management[Blueprint active]/Experience-Center), Blueprints/Filters/Usage tabs, description + draft-note, Search/Reorder/Create-Blueprint (teal), table (Lead Creation/Deals/Donor Management with Module/Layout/Field/Last-Modified/Status toggle); (2) **Create new Blueprint modal** — Blueprint name (deals), Module (Potentials), Layout, Choose field (Stage, teal underline), Pipeline, criteria row, Description, Advanced configuration, Cancel/Next (teal); (3) **Canvas** — dotted bg with "Simply drag & drop States / Start" dashed circle + right panel "Info and States" (name, Continuous toggle, +Description, Module/Layout/Field/Pipeline, Entry criteria/Add Criteria, Available States list: Qualification…Closed Lost to Competition) + Publish/Save-as-Draft/Cancel/Export bar; (4) **Transitions** tab — transition "saas" between Qualification→Needs Analysis, Common Transition, **BEFORE/DURING/AFTER** tabs (BEFORE: Owners=Record Owner + Criteria; DURING: add field; **AFTER: Email Notifications with Select Template**). **The email-template association** is the point: AFTER-tab "Select Template" reuses the existing template picker (`openENTplPicker('bp-after-tpl')` — made the picker target-aware via `enTplTarget`), and picking a template shows "✓ Email template <name> associated with this transition's emails" (`bpAfterTplChanged`). Right panel toggles Info↔Transitions tabs; canvas shows the empty Start circle on Info, the state nodes on Transitions. Why: user asked to make Blueprint clickable and reproduce the 5 screenshots exactly, the idea being an email template gets associated to the transition's emails. Verified live at localhost:8080 (list→create→canvas→transition AFTER→template associated; no console errors).
- 2026-08-24 — [branch `create-flow-v2`] On the survey detail page: added an **open-in-new-page (external-link) icon next to the "Edit" button**, and wired Edit to `sdEditInBuilder()` → opens the **survey builder** (`#svc-builder`, reused from the create flow) with the current survey's name (hides the detail page while editing; sets `svcEditMode=true`). `svcDone()` now branches on `svcEditMode`: in edit mode it just closes the builder and re-opens the **same** survey's detail unchanged (preserves its data/fresh state), instead of creating a new Draft. Refactored the builder-show into `svcRenderBuilder(name)` shared by create + edit. Also **removed the "Add Collector" button from the View Survey Link panel** (`openSurveyLinkPanel`) — the collectors table remains. Why: user asked for an Edit affordance that signals it opens the builder in a new page (and to actually show the builder), and to drop Add Collector from the survey-link view. Verified live at localhost:8080 (Edit→builder with name, Done→same detail w/ Draft badge; Survey Link panel has no Add Collector; no console errors).
- 2026-08-24 — [branch `create-flow-v2`] Per the manager decision to **not build a CRM-style create flow and instead reuse Zoho Survey's own create flow**, re-pointed the survey-list "Create Survey" button from the old `openCreateSurvey()` (CRM-style page, left intact as fallback) to a new `openSvCreate()` survey-native flow reproducing the 3 Zoho-Survey screenshots, wrapped in a slim CRM chrome bar: (1) **landing** (`#svc-landing`) — "Choose your preferred way to create the survey" with top tabs (Create New Survey/Using AI/Import/Choose from Template/Copy Existing) + 3 cards (Create from scratch / Quick Editor / Create using AI); (2) **Create Survey modal** (`#svc-modal`) — Survey Name (teal focus), Category dropdown, Add Tag, black CREATE / grey CANCEL; (3) **Builder** (`#svc-builder`) — Survey header, Editor/Settings/Themes/Hub/Publish tabs, PREVIEW, dark Summary/Builder/Audit rail, 17-row Question Types panel (`SVC_QTYPES`), black Page-1/Untitled/Logic canvas bar with "Drag and drop / Paste your questions here", and PAGE(1)/P1 thumb. **Handling "no Save" in CRM:** the survey product has no Save (auto-saves; terminal step is Publish), so CRM owns the exit — a **"Done"** button in the CRM chrome bar. Done opens a **publish-nudge modal** (`#svc-donemodal`, option B): "Publish before you go?" → "Publish & Return" or "Return as Draft", both call `svcReturn(published)` → closes the create flow and **reuses the existing `openSurveyDetail()` survey detail page** (passing the created name + category from `svcDraft`, status Draft/Published), landing the user on the new survey's Overview. **Empty-first insights:** a freshly created survey (`fresh:true`) shows zeroed insights until it's sent — new `sdSetData(hasData)` toggles the 6 Response-Statistics tiles (0 vs 37/37/13/2/0/11), hides the three insight sections (`#sd-reports-section`/`#sd-respondents-section`/`#sd-qresp-section`), shows a `#sd-empty-note` ("No responses yet — send the survey…"), and empties the invited list; `openSurveyDetail` calls `sdSetData(!survey.fresh)`. When a send **completes** from a fresh survey's detail page, `ssSendMassEmail` calls `sdMarkSent()` (sets `fresh=false`, re-renders) so the data populates. **Follow-up fixes:** the create-flow "Done" wasn't passing `fresh:true`, so new surveys still showed data — fixed `svcDone()` to open the detail with `fresh:true` + `status:'Draft'`; **removed the publish/draft nudge dialog** (deleted `#svc-donemodal` + `svcReturn`; Done goes straight to the detail page); and added a **Draft/Published status badge next to the survey name** (`#sd-survey-status`, amber Draft / green Published, driven by `survey.status` in `openSurveyDetail`). Also **removed Advanced Settings from the Send Survey flow** (hid the `ss-adv-toggle` field-row — collector + custom-var refs kept in DOM so dependent JS doesn't break) and **guarded a pre-existing crash** (`ssShowToast` was referenced but never defined — wrapped in a typeof check so send completion no longer throws). Chrome bar also shows "Back to Zoho CRM" + an "Auto-saved" indicator. Why: manager decided to reuse the survey's create flow (less effort, always current); the design question was how to show it in CRM and reconcile the missing Save — answer: embed the survey UI full-screen under CRM chrome, and have CRM provide the Done/return (the survey is already persisted the moment "Create" is clicked + auto-save, so Done is an exit, not a save). Verified live at localhost:8080 (landing→modal→builder→Done nudge all render faithfully; 17 question types; no console errors). NOTE: experimental — master keeps the old CRM-style create flow.
- 2026-08-24 — Per the design decision to **not duplicate a "Send Survey" action** (a survey link inside the existing Notify/Email-Notification flow does the same job — response ties back via custom variables), **removed "Send Survey" from the Workflow Add-Action flyout** and **wired the flyout's "Notify" item to open a full Email Notification flow** (`wfFlyoutPick('Notify')` → `openENPicker()`; the old `wfFlyoutPick('Send Survey')` branch and the flyout `<div>` are gone — the Send-Survey list/new modals remain in the file but are now unreferenced). Built the Email Notification flow as an exact reproduction of the user's 5 desktop screenshots (`#en-picker`/`#en-form`/`#en-tplpicker` overlays + `<script>`, inserted before `#analytics-page`): (1) **picker** — "Email Notification - {module}" title, Search, magenta "New Email Notification" button, Name/Email-Template/Modified-On table (7 sample rows: Notify NPO User…revenue), row-select enables Associate; (2) **New form** — Name (required), To (Choose Users/Groups/Roles + people icon), "Single Mass Email" checkbox, CC, BCC link, Email Template (Select Template btn + selected name in magenta), From (ssunderjan@gmail.com) + yellow "Sender email changed" warning, Reply To, "Best Time to Email" checkbox, Cancel/Save-and-Associate — combines screenshots 2+5; (3) **To dropdown** — Lead/Search, "People Associated with the Module"→Lead(checked), "Other People"→Groups/Roles, "Add additional recipient"; (4) **Select Template modal** — All Templates dropdown, Search, A→Z, Create Template, 9-row list with folder tags + Open-Rate/Preview on hover. Both Associate (from picker) and Save-and-Associate (from form) append an `{type:'Email Notification', detail}` action to the active rule via `renderWfActionsList()`. Module badge is pulled live from the active rule. Why: user concluded a separate Send Survey action offers nothing over Notify + a survey-link template (same job, response still ties back), so it shouldn't be duplicated — remove it and make Notify open the real Email Notification screens exactly per the 5 screenshots. Verified live at localhost:8080 (flyout no longer lists Send Survey; Notify → picker → New form → Select Template all render faithfully; no console errors).
- 2026-08-24 — Built the **Customization → Templates → Insert-Survey-Link flow** as an exact reproduction of the user's 5 desktop screenshots (`#ctmpl-page`/`#ctmpl-preview`/`#tpl-editor`/`#tpl-linkmodal` overlays + `<script>` block, inserted before `#analytics-page`), and made the **Setup Home → Customization → "Templates"** link clickable (`openTemplatesPage()`). Screens: (1) **Templates list** — Setup left rail (Customization expanded, Templates highlighted, plus Channels/Automation groups), Email/Inventory/Mail-Merge pill tabs, category list (All Templates…Patient Education) + New Template button, and a Template-Name/Modified-By/Last-Used/Stats table with the screenshot's 9 rows (Zoho Survey row highlighted), each row → preview; (2) **Preview panel** — Desktop/Mobile toggle, Preview/Analytics tabs, "Mail data with Masking", template body (`Hello ${Leads.Last Name}` + "Is the product meeting…" radios), edit/clone circle buttons; (3) **Editor** — dark header (name + Leads tag, Attachments/Cancel/Preview/Save), rich-text toolbar w/ link icon, body, yellow merge-field HINT bar; (4) **Insert Link (Web URL)** — Link Type, Selected Text, URL, Title, Include-SalesIQ checkbox + dashed box; (5) **Insert Link (Zoho Survey)** — Link Type=Zoho Survey, Select the Survey dropdown, Survey Link (readonly + Preview), Text To Display — **and below it, per the request, Choose Collector + Custom Variable (Add-parameter rows) lifted from the Send Survey flow** (reuses `ssCustomVars`/`ssModuleFields.Leads`; own `tplParamRows` state). Link Type dropdown toggles Web-URL vs Survey field sets; survey dropdown updates the link. Fixed an **ID collision**: the mock already had a `#tpl-preview` (survey-builder slideover) so `getElementById` grabbed the wrong node — renamed my preview overlay to `#ctmpl-preview` (only that one collided; all other `tpl-*` ids/functions were unique). Why: user asked to make Customization→Templates clickable and reproduce the 5 screenshots exactly, with the Zoho-Survey Insert-Link page showing collector + custom variable below Text-to-Display (reference the Send Survey flow) — the survey-link insertion path that feeds the email-template send. Verified live at localhost:8080 (list→preview→editor→insert-link Web/Survey all render, collector + param rows work; no console errors).
- 2026-08-20 — Rebuilt the **Analytics → Survey Dashboard into a "Customer Voice" manager dashboard** designed around the showroom manager's two jobs (handle bad feedback / reward good feedback), replacing the old flat KPI-band + per-deal table + 5 illustrative charts. Everything is data-driven from a single `cvResp` response set (13 responses across the three auto-send touchpoints — **Test Drive / Delivery / Service** — each carrying deal ₹, CSAT/NPS/rating/score, owner, service center, recency, trend, and verbatim). Five labeled sections via a new `cvSec()` divider: **(1) Handle bad feedback** — hero band (At-Risk Revenue ₹ [PRD North Star framing], # at-risk deals, revenue-weighted sentiment /100, Recovered ₹ w/ NORTH STAR tag, all with ▲/▼ vs last wk), a revenue-ranked **At-Risk Deals worklist** (Customer/Touchpoint tag/Model/₹-at-risk/Score/Trend/Owner/Last-response, sorted by ₹ desc), a **"The Voice" detractor verbatim feed**, and **Objection themes** (₹-exposure bars); **(2) Metrics** — a full-width **Sentiment-by-touchpoint** card, one color-top-bordered column per touchpoint showing sent/responded/rate/coverage + CSAT/NPS/rating + promoter/detractor counts + at-risk ₹; **(3) Trends** — new `anLine()` multi-series SVG line helper drives **At-Risk vs Recovered Revenue** (2 lines) and **CSAT trend by touchpoint** (3 lines, Service sliding); **(4) Root cause** — avg-score-by-model, by-service-center, avg-CSAT-by-consultant bars (red/green by threshold) + NPS distribution pie; **(5) Reward good feedback** — **consultant leaderboard** (rank/CSAT/NPS/promoters/detractors/₹-at-risk, sorted by CSAT), **promoters & referral opportunities** list (Ask-for-review / Referral-lead badges), and **testimonials** (top promoter quotes). Added `anLine()` + helpers (`cvL/cvTag/cvSec/cvArrow/cvTpStats/cvReps`) and data (`cvResp/CV_TP/CV_SENT/cvThemes`); Leads dashboard untouched. Deliberately excluded operational extras (alerting, delegate/bulk-assign, close-loop tracking, OEM-bonus gauge, SLA, live filters/drill-through) per user's "except E" scope. Why: user (thinking as the car-showroom/Sales-Manager persona) asked what the manager needs to see to handle bad feedback and reward good feedback across auto-sent Test-Drive/Delivery/Service surveys — metrics, analytics, trends — and to implement all of it except the operational-extras bucket, minimalist and granular. Verified live at localhost:8080 (all 19 blocks render across 5 sections, line/bar/pie charts, no console errors).
- 2026-08-19 — Made the Deal flash cards **aggregate latest-per-metric across ALL surveys** (not one survey) and **removed the glowing new-response alert**. `renderDealSurveys` now builds an `agg` map over responded surveys (chronological → last one wins = latest) with a per-metric count; each metric card (NPS/CSAT/Avg Rating/Score, show-if-present) shows the latest value + a subtext "latest · N surveys" (or "1 survey"). Reworked `dealSurveysData` into a genuine 3-survey scenario with overlapping metrics — Test-Drive (NPS 9/CSAT 4.6/Rating 4.5, 13 Aug), Proposal Fit (Score 58%, 18 Aug), Post-Delivery CSAT (CSAT 3.1/Rating 3.0, 22 Aug) — so CSAT & Rating each come from 2 surveys and the card shows the latest (CSAT 3.1, not 4.6). Coverage now reads "all responded" when none pending; Last response subtext = "most recent of N". Dropped the `cv-alert` block and trimmed `dealSurveyMeta` to {lastAgo, speed}. Why: user asked how to show metrics when 3 surveys are sent to a deal (answer: latest-per-metric) and to remove the glowing badge. Verified live at localhost:8080 (8 cards aggregate across 3 surveys, no alert; no console errors).
- 2026-08-19 — Trimmed the Deal "Zoho Survey · Signals" section per user: removed the **Sentiment** flash card (1), the **Referral** flash card (14), the **✓ Loved / ✗ Weakest** flash cards (7), the **Latest + weakest-answer + verbatim + Next-step insight box**, and the entire **"Surveys sent to this deal" timeline** (per-survey rows + per-question drill-down). What remains: the pulsing new-response alert banner + 6 flash cards (NPS class, CSAT-vs-your-avg benchmark, Coverage, Last response recency w/ live dot, Reply speed, Touchpoints). The separate "Zoho Survey" related-list table (Title/Sent/Responded/Source/View Response) below is untouched. Why: user asked to remove those specific elements. Verified live at localhost:8080 (6 cards + alert; no console errors).
- 2026-08-19 — Added a **flashy flash-card row + pulsing new-response alert** to the top of the Deal "Zoho Survey · Signals" section (built into `renderDealSurveys`, computed from `dealSurveysData` + new `dealSurveyMeta`). Approved card set 1,2,7,9,10,11,12,13,14,15: (1) Overall **sentiment badge** (Watch/At-Risk/Healthy, emoji + colour, worst-active across responded surveys); (2) **NPS class** tag (Detractor/Passive/Promoter from the NPS metric); (7) **✓ Loved / ✗ Weakest** cards (best & worst per-question answer across surveys); (9) **Coverage** (responded/total + pending); (10) **Last response** recency with a pulsing green "live" dot; (11) a **pulsing red new-response alert banner** ("New response · 2h ago — Proposal Fit 58%… REVIEW", `@keyframes cvPulse`); (12) **Reply speed** ("4 min · engaged"); (13) **CSAT vs your avg** benchmark (4.2 ▲ vs team 3.8); (14) **Referral** willingness (Likely 👍 from NPS); (15) **Touchpoints** (# surveys over the deal). Added CSS keyframes cvPulse/cvDot/cvRise + `.cv-fcard/.cv-alert/.cv-live` classes. Sits above the existing insight strip + survey timeline. Why: user asked the deal's survey flash cards to be flashy and hook the rep, and picked exactly this card set beyond CSAT/NPS/Rating/Score. Verified live at localhost:8080 (10 cards + pulsing alert render; no console errors).
- 2026-08-19 — Redesigned the Deal "Zoho Survey · Signals" section from a single 4-tile snapshot into a **multi-survey timeline with per-question drill-down** (data-driven via `dealSurveysData` + `renderDealSurveys()`, called from `openDealDetailPage`). A deal now shows every survey sent to it (Test-Drive Experience, Proposal Fit Score, Relationship NPS[pending]) as expandable rows, each carrying only the metrics it collected (show-if-present chips: Rating/CSAT/NPS on one, Score on another). An **actionable insight strip** on top uses the latest responded survey (no trend — per user, a single record doesn't need the survey-wide trend): Latest score + band, the **weakest answer** (min question ratio, e.g. "Pricing vs. budget fit 2/5"), the open-text verbatim, and a Next-step chip. Expanding a survey (`dsToggle`) reveals a **per-question table** (Question | Answer/Score with a mini bar, colour-banded) + the open text — mirroring Zoho Survey's Overall NPS / Average Ratings / Overall CSAT report breakdowns the user shared, surfacing *which question* drags the cumulative score. Pending surveys show "Yet to respond" and don't expand. Why: user said the prior single cumulative CSAT tile was too basic, that a deal can have multiple surveys, and asked for actionable insights + per-question detail (since the headline scores are cumulative across questions and responses); confirmed no per-record trend. Verified live at localhost:8080 (3-survey timeline, expand shows weak-question breakdown, pending state; no console errors).
- 2026-08-19 — Added a CRM **Analytics / Dashboards** page (`#analytics-page`) replicating the user's Zoho CRM Dashboards screenshot exactly: navy left rail (Home/Workqueue/Reports/**Dashboards**(active)/Agents + "RouteIQ for Zoho CRM"), white topbar ("Dashboards" title + Search records + pink + button), toolbar (All▾ · star **dashboard switcher** · All Users · refresh · pink filter · Add Component · pink "Create Dashboard" · ⋯), and a "Created By (All Modules) / Filter By / Apply / Apply and Save" filter row. New "Analytics" sidebar entry (in the main white module nav) opens it. Default view = **Lead Dashboards** (4 cards from the screenshot: Leads By Status green bar, Sales by Lead Source pie+legend, Leads by Industry bar, Call Status Report pie — inline-SVG `anBar`/`anPie` helpers, "Created from report - …" footers). The switcher menu offers **Survey Dashboard** (NEW): a full-width "Customer Voice — Today's Services (Sundaram Kia)" card with KPI tiles (42 sent / 30 responded / 71% / Avg CSAT 3.8 / Avg NPS 7 / Avg Rating 3.6 / 6 detractors) + a per-deal table (Deal/Contact/Model/Service Date/**CSAT/NPS/Rating/Score**/Owner, color-coded), then chart cards (Service CSAT by Car Model, NPS Distribution pie, Responses by Survey Type, Avg Rating by Service Center, Survey Score Bands pie). Also: **removed the "Deal Health · Survey Signals" composite panel** from the Deal detail and replaced it with a simple **"Zoho Survey · Signals"** strip of 4 show-if-present metric tiles (CSAT 4.2/5 · NPS 8/10 · Rating 3.0/5 · Score 58%) + a show-if-present note (related-list table kept; `renderDealSignalsTable` guarded for the removed tbody). Added the same metric strip to the **Lead detail** Zoho Survey section (CSAT 3.8 · NPS 7 · Rating 4.0 — no Score tile, demonstrating show-if-present). Why: user asked to put the manager's survey analytics inside Analytics (Leads dashboard default, switch to Survey dashboard, exact screenshot design), show the per-record CSAT/NPS/Rating/Score on Leads and Deals, and replace the old deal-signals panel with this simpler model. Verified live at localhost:8080 (both dashboards + switcher, deal strip, lead strip; no console errors).
- 2026-08-19 — Reverted the 2026-08-19 rep-problem build (alert banner, Revenue-at-Risk-this-deal stat, sentiment trend, verbatim, stakeholder coverage, Next Best Action + auto-task + recovery loop) from the Deal detail Survey Signals panel — mock-reference.html restored to its prior committed state. Why: user asked to revert it completely and restart from a simpler framing (rep sees a deal's CSAT/NPS/Rating/Score if present; manager sees the same across many deals; contact shows its own data).
- 2026-08-18 — Made the sidebar "Deals" module item clickable, opening a new Potentials list page (`#deals-list-page`) reproduced from the user's screenshot of the real Zoho CRM Potentials module (All-Territories dropdown, All Potentials tab, Filter/Sort toolbar + Create Potential split button + Map View, left "Filter Potentials by" panel, and a Product Recommend/Potential Name/Amount/Stage/Closing Date/Account Name table incl. KIA-SEL-002). Clicking KIA-SEL-002 opens a new Deal detail page (`#deal-detail-page`) reproduced from the screenshots: the Blueprint stage strip (Qualification→…→Value Proposition[active]→…→Closed Lost to Competition + thumbs up/down), Overview/Timeline tabs, Stage/Probability/Expected Revenue/Closing fields, Best-time card, Contact Person, the left Related List nav + SALES SUMMARY, a **Deal Health · Survey Signals** panel (workflow-driven metric-agnostic design: health gauge + At-Risk badge + coverage chips + a workflow/stage touchpoints table showing whatever metric each fired survey returned — CSAT 4.2/Score 58%/Rating 3.0/NPS pending — worst-active driving the signal), and the **Zoho Survey related list** (Title/Sent Date/Responded Date/Sent By/Source/Response w/ View Response). Why: user asked to make Deals clickable, show the Potentials list + deal detail matching the screenshots, and surface survey signals per transition/workflow inside the deal.
- 2026-08-06 — Removed the "Skip this respondent if already sent in the last 30 days" checkbox from both the Send Survey list and New modals; renamed "Select Survey" to "Select Template"; added a "To" field below Name in the New modal, prefilled with a read-only-styled sample email address. Why: user asked to drop that checkbox, rename the label, and add a prefilled To/email field matching the Email Notification reference pattern.
- 2026-08-06 — Wired "Send Survey" in the workflow Add Action flyout to a two-step config flow modeled on the user's Email Notification screenshots: picking it opens a "Send Survey - {Module}" list modal (search box, magenta "New" button, Name/Survey/Modified On table, a "skip if already sent in last 30 days" checkbox row, Cancel/Associate) instead of adding the action directly; selecting a row and clicking Associate appends that config to Instant Actions. "New" opens a second modal ("Send Survey - {Module}": pink-bordered required Name + Select Survey fields, a "Best Time" checkbox, and an "Advanced Settings" section reusing the Lead detail Send Survey pattern — Choose Collector picklist + Custom Variable/"Add parameter" rows) with a magenta "Save and Associate" button that both saves the config and appends the action. Restyled both modals' primary buttons to solid magenta (#A02059) and added the sort caret + checkbox rows to match the screenshots more exactly after the first pass was flagged as not matching closely enough. Why: user asked for the Send Survey action to open these exact screens, with Advanced Settings (collector + custom variables) referencing the existing Send Survey details view.
- 2026-08-06 — Replaced the "Add Action" modal on the workflow rule setup page with an exact replica of the user's screenshot: clicking "+ Action" now opens a small inline flyout menu anchored directly below the Instant Actions card (not a centered modal), giving the card a pink border while open, listing Field Update/Assign Owner/Tags▸/Notify▸/Activities▸/Create Record/Create Connected Record/Webhook/Function/Actions By Zoho Flow — with one addition, "Send Survey" (marked "New"), inserted into that same list. Picking "Send Survey" appends it to the rule's Instant Actions; the other items are non-functional placeholders matching the real menu's shape. Why: user asked to replicate the exact "Add Action" screenshot with Send Survey added into that list.
- 2026-08-06 — Rebuilt the workflow rule setup page as an exact replica of the user's screenshot of the real Zoho CRM rule-builder canvas: dotted light-beige background, a "WHEN" circle node connected to a pink-bordered box with the dynamic "This rule will be executed whenever a {module} is created/edited..." sentence, a "CONDITION 1" diamond node connected to "This rule will be executed for all {module plural}.", an "Instant Actions" card listing the rule's configured actions with a "+ Action" row (still opens the existing action-type picker), a dashed "Scheduled Actions" placeholder, header controls (lock icon, "Add Description", Rule is Active/Deactivated text + Activate/Deactivate toggle + View Usage + "..."), and a sticky Save/Cancel footer. Module names are mapped to singular/plural (deal/deals, lead/leads, etc.) so the sentence reads correctly for any rule. Why: user asked to replicate the exact screenshot of what shows after clicking into the first workflow.
- 2026-08-06 — Rebuilt the Workflow Rules list page to replicate the user's screenshot of the real Zoho CRM Workflow Rules screen: real app topbar with a "← Setup Home" breadcrumb, a left Setup nav sidebar (Automation expanded, Workflow Rules highlighted), Rules/Usage tabs, the "Simply Build Data Processing!" Catalyst promo banner with an Explore Now button, a search box + "Workflow Creation using Zia" link + disabled "Reorder Rules" + "Create Rule" button row, and a table with checkbox/Rule Name/All▾(module)/Execute On/Actions(count)/Modified On(avatar+date)/Status(working green toggle switch) columns populated with the screenshot's real rule names (create_dbx_folder_for_lead, addEventsToBookings, Create lead, etc.) plus our own "Send CSAT Survey after Deal Closed-Won" rule carrying the Send Survey action. Why: user asked for an exact replication of the reference screenshot once a workflow rule is clicked into from Setup.
- 2026-08-06 — Removed the standalone "Actions" link under Setup Home's Automation category (it opened a generic action-types catalog page). "Workflow Rules" stays clickable, and the "Add Action" flow inside a workflow rule's setup page (which lists the same action types, including Send Survey) is unchanged. Why: user confirmed Settings → Setup is working correctly and asked to remove the separate Actions entry below Workflow Rules.
- 2026-08-06 — Wired up the Automation category on Setup Home: "Workflow Rules" now opens a Workflow Rules list page (Rule Name/Module/When/Executed On/Status); clicking a rule opens its setup page (When trigger card + Actions list with an "Add Action" button); Add Action opens a "Choose Action Type" overlay listing Email Notification/Task/Field Update/Webhook/Function plus a new **Send Survey** action type (marked "New"), and picking one appends it to that rule's Actions list. "Actions" on Setup Home also opens a standalone Actions catalog page with the same action types, including Send Survey. Why: user confirmed the chain Settings → Workflow Rules list → a workflow's setup page → Actions list → add Send Survey as a new action type — this is the mechanism that lets a CRM automation trigger (e.g. "Deal moves to Closed-Won") auto-send a survey, closing the loop discussed earlier between sending a survey and acting on business events.
- 2026-08-06 — Added a "Send Survey" button next to "Send Mail" in the Leads list bulk-selection action bar. Clicking it opens the existing Mass Email modal (matching the user's screenshot: To/Template/From/Send Options/Advanced Settings/Send) directly, but with the "To" field populated with the names of the bulk-selected records (e.g. "MiBonban, Mi Mau, Nice Karen") instead of the "+ Add Recipient" picker button, since the recipients are already fixed by the list selection. Why: user asked for a Send Survey bulk action that reuses the Mass Email screenshot's layout, with selected record names pre-filled in To.
- 2026-08-06 — Added an "Advanced Settings" collapsible section to the Send Survey modal on the Lead detail page, containing a "Choose Collector" dropdown and a dynamic "Url Parameter" row list (Add parameter / per-row Parameter Name + Parameter Value selects / +/× row controls) — mirroring the same Advanced Settings pattern already used in the survey module's Mass Email send flow (reusing its `ssCustomVars`/`ssModuleFields.Leads` option lists), implemented as its own local state (`ldParamRows`) so it doesn't interfere with the module flow's state. Why: user asked to add collector + URL param fields to the individual-record Send Survey flow, matching the existing survey-module implementation.
- 2026-08-06 — Removed the "Create Survey" and "Take Survey" buttons from the Lead detail page's Zoho Survey section, leaving only "Send Survey". Why: user asked to remove those two buttons.
- 2026-08-05 — Gave "View Response" its own "Response" column header (it had been embedded inline inside the Title cell with no labeled column), positioned as the 2nd column right after Title. Why: user pointed out View Response had no column name.
- 2026-08-05 — Reworked the Zoho Survey related-list table on the Lead detail page: "View Response" moved from its own far-right Response column to sit directly next to the survey Title; Responded Date is now filled in (was blank) since a response is simulated as already in; and added three new columns — NPS Score, CSAT Score, Total Rating — sourced from the same response data shown in the View Response box. Why: user asked for the responded date to be filled, View Response moved next to the title, and NPS/CSAT/Total Rating added as columns.
- 2026-08-05 — Restyled the Questions list inside the View Response box: replaced the 3-column table (Questions/Question Scoring/Answer) with a stacked per-question row matching the user's screenshot — question text on top, the respondent's answer directly below it, and the Question Scoring badge aligned to the right of that stacked block. Why: user said the scoring column layout didn't match the screenshot; question should be above, answer below, scoring on the right.
- 2026-08-05 — Wired up "Send Survey" from the Lead detail page's Zoho Survey related-list section, reproducing the user's 8 reference screenshots: a Send Survey modal (survey dropdown, auto-filled read-only Survey Link + Preview, optional Text To Display, Cancel/Insert), which on Insert opens a mail-compose popup (To chip, Subject, body with the display text as a hyperlink + signature, Send). Sending populates the Zoho Survey section with a real table (Title/Sent Date/Responded Date/Sent By/Source/Response) — but per explicit instruction the Response column shows a clickable "View Response" link instead of the screenshot's literal "Yet to Respond". Clicking "View Response" opens a Response Details box with the individual respondent's fields (Name/Email/Module/Response ID/IP/User agent/Survey URL/Start & Completion time/Time taken/Collector/Total rating/NPS/CSAT/Average rating/Score) plus a Questions table with a new "Question Scoring" column (colored score badge) alongside each question's answer. Why: user asked to build the Send Survey flow on the individual record detail page per the screenshots, with "View Response" replacing "Yet to Respond" and an added Question Scoring column.
- 2026-08-05 — Made "Mi Mau" clickable in the Leads list, opening a new Lead detail page (`#lead-detail-page`) reproduced from the user's screenshots: header (avatar, name, Add Tags, Send Mail/Convert/Edit/Add File to Dropbox actions), Overview/Timeline/Data Privacy tabs with "Last Update" timestamp, Mobile/Lead Source fields, "Best time to" and "Similar Leads" cards, and the related-list sections below (Visits - Zoho SalesIQ, ZohoSign Documents, solutions, Dropbox Files, Remote Assist, Zoho Survey with Create/Send/Take Survey buttons) matching the screenshot content. Clicking "Zoho Survey" in the left Related List nav now smooth-scrolls all the way down to that section, matching the third screenshot's highlighted state. Also added a bulk-selection action bar to the Leads list page: checking any row (or the header checkbox) replaces the toolbar with "N Records Selected. Clear | Select All records in this view" plus Send Mail/Tags/Send with Zoho Sign/more buttons, and selected rows get the pink highlight — reproduced from the fourth screenshot. Why: user asked to make Mi Mau and the Zoho Survey related-list item clickable per the latest screenshots, and to show the bulk-action bar on multi-select.
- 2026-08-05 — Made the sidebar "Leads" module item clickable, opening a new full-screen Leads list page (`#leads-list-page`) reproduced from the user's latest screenshots of the real Zoho CRM Leads module: same topbar, "All Leads"/"Converted Leads" tabs, Filter/Sort/view-icon toolbar with a "Create Lead" split button, a left "Filter Leads by" panel (System Defined Filters + Website Activity checkbox groups), and a table (Lead Name/Lead Owner/Email/First Name/Created By) populated with the exact 19 sample rows visible in the screenshots, with the same pink row-hover highlight. Back arrow returns to the Zoho Survey list. Why: user asked to make "Leads" in the list view clickable and match the latest desktop screenshot for what shows when clicked.
- 2026-07-30 — Rebuilt the Setup Home and Marketplace-Zoho pages added earlier the same day to match the user's two reference screenshots far more closely: Setup Home now reuses the real app topbar (search/icons/avatar row) with a bare back arrow instead of a text header, a "Setup"-title-plus-search-plus-"Customize Setup"-button row, and the 10 categories laid out as two bordered 5-column blocks with per-category line icons and internal column dividers, matching the screenshot's grouping and spacing. The Marketplace-Zoho page now has a "← Setup Home" breadcrumb header (colored to match the account's accent), a left-hand Setup navigation sidebar (grouped by category, auto-scrolled to and highlighting the active "Zoho" item under Marketplace) alongside the six app cards, replacing the emoji icons with outline SVG icons and a bordered 3-column card grid. Why: user said the first version didn't do justice to the screenshots — alignment and structure were off — and asked to match them as closely as possible.
- 2026-07-30 — Wired up the top-bar settings (gear) icon to open a new full-screen "Setup Home" page listing CRM setting categories (General, Security Control, Customization, Channels, Automation, Process Management, Experience Center, Data Administration, Marketplace, Developer Hub) reproduced from the user's screenshot. Clicking "Zoho" under Marketplace opens a second new page, "Marketplace — Zoho", showing the six Zoho app integration cards (Backstage, Meeting, Projects, Desk, Survey, Cliq) reproduced from the user's second screenshot, each with its description and Manage/Setup/Continue button. Both are new top-level fixed-position overlay pages (`#setup-page`, `#marketplace-zoho-page`) with back-arrow navigation (Setup → survey list, Marketplace-Zoho → Setup). Why: user shared two screenshots and asked that clicking Settings show the setup list, and clicking Zoho from there show the marketplace apps screen.
- 2026-07-30 — Reverted the Survey Quota body (progress bar + meta labels) back to the original flat linear bar with the "142 / 200" fraction and Limit/When Reached/Remaining labels, undoing both the radial-ring redesign and its simplified follow-up from earlier the same day. Kept the "View Conditions" button and its real-condition popover exactly as they are now (positioned next to the "Survey Quota" title, showing the survey's actual configured condition(s) on click) — only the bar and the elements below it were reverted. Why: user asked to keep View Conditions as-is but revert the bar and everything below it to how it looked at the start.
- 2026-07-30 — Simplified the Survey Quota section redesigned earlier the same day: removed the duplicated 3-tile meta grid and the always-on percentage badge (both restated the same number already shown in the ring), replacing them with one plain-language sentence ("142 of 200 responses collected") plus a single secondary line ("58 responses left before the quota is reached • When reached: Close the survey"). The status badge now only appears as a warning — "Almost full" at ≥90% collected, "Quota reached" at 100% — and is hidden otherwise instead of restating the ring's percentage. Verified on 71% (no badge), 86% (no badge), and reused the reached/empty-state cases from the earlier redesign. Why: user said the redesigned version was confusing and asked for something easier to understand.
- 2026-07-30 — Redesigned the Survey Quota section (below Response Statistics): replaced the flat linear progress bar with an SVG radial progress ring + percentage, a fraction line ("142 / 200 responses collected"), and a 3-tile meta grid (Limit/When Reached/Remaining) matching the Response Statistics tile style; added a status badge on the far right (color-coded by how close to quota). Moved "View Conditions" from a far-right link (easy to miss) to sit directly next to the "Survey Quota" title. Clicking it now opens a read-only popover showing the survey's actual configured quota condition(s) — pulled from a new `quotaConditions` field on each survey object — rendering multiple conditions with an "AND" divider, or a "No condition set" empty state when none are configured. Verified against 3 cases: single condition (Customer Satisfaction Survey Q1 2025), two AND-chained conditions (Employee NPS — Q1 2025), and no conditions (Product Onboarding Feedback). Why: user asked for a different visual treatment of the quota section, said the old "View Conditions" placement was easy to miss, and wanted it to actually show the configured condition instead of a generic/blank editor.

- 2026-07-30 — Extended chart-to-table cross-filtering into the "Details" expandable tables of Overall CSAT Score and Average Ratings. Replaced their dummy travel-themed question rows with the survey's real questions (qSatisfied/qMeeting/qLimitations/qFollowup/qRate) and real Yes/No/rating counts computed from `sdRespData`; clicking a Yes/No/rating count now filters All Respondents the same way the other charts do. Why: user asked that clicking a question in these two cards' expanded Details should filter the table too.

- 2026-07-30 — Made the first 4 Survey Reports charts (Responses by Module, Responses by Collector, Daily Responses Trend, Overall CSAT Score) cross-filter the All Respondents table: clicking a legend/bar/point/row scrolls to and filters the table to matching respondents, with a dismissible chip ("Module: Leads ×") replacing the earlier plan of a plain Clear link. Reconciled each chart's mock numbers/colors to actually match `sdRespData` (6 respondents) so the filtered counts are truthful instead of the old illustrative-only totals. Skipped the 5th chart (Average Ratings, per-question) — it has no per-respondent field to filter by (all 6 respondents answer every question), so a row filter there wouldn't narrow anything; flagged to user rather than faking it. Why: user asked for chart-to-table cross-filtering with an X-to-clear affordance.

- 2026-07-30 — Reverted the "Pending Responses" and "CSAT & NPS by Account" cards added earlier the same day. Why: user asked to remove them after reviewing.
- 2026-07-30 — Added two new Survey Reports cards on the survey detail page: "Pending Responses" (sent-but-not-responded count + preview list + per-row "Remind"/"Remind All") and "CSAT & NPS by Account" (per-account CSAT/NPS snapshot with a trend arrow and a flagged-account callout). Why: closes gaps against the persona doc's stated needs ("who hasn't responded yet", "CSAT/NPS trends per account") that weren't covered by the existing aggregate-only cards.
- 2026-07-29 — Fixed Night-mode dark theme to cover every page, not just the survey list. Root cause: the invert+hue-rotate filter was scoped to `.app` (sidebar+list only) — `#create-survey-page`, `#form-builder-page`, `#survey-detail-page`, and the various modal overlays are separate top-level siblings of `.app` under `<body>`, so they were untouched. Rescoped the dark-mode rule to `body.app-dark-mode > *:not(#um-overlay)` so it covers every top-level page/overlay while leaving the (already-dark) account menu panel alone. Why: user reported only the list page went dark.

- 2026-07-29 — Added the account/user menu (click the "M" avatar top-right): profile, org switcher, plan card, Day/Night/Auto mode toggle, theme swatches, Need Help/News room/Mobile App sections, My Account/Sign Out footer. Night mode now applies an app-wide dark theme (invert+hue-rotate filter on `.app`, double-applied to `<img>` to cancel it). Why: user provided a screenshot of the desired account panel and asked for the dark theme toggle to actually work.
- 2026-07-23 — Replaced the left sidebar with the Figma "Nxt Gen Left Menu" design (white bg, static nav section, CRM Teamspace switcher, Search Modules box, icon-led module list, collapsible Activity/Inventory/Projects/Cliq Channels folders). Why: user asked to apply that Figma section to the list view page's module nav.
- 2026-07-23 — Repo initialized and pushed; began tracking mock edits here going forward.
